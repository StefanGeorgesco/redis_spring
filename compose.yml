services:
  redis:
    container_name: redis
    hostname: redis
    image: "redis:latest"
    ports:
      - "6379:6379"

  city-service:
    image: "city-api-external-services"
    ports:
      - "3030:3030"